import js from '@eslint/js';
import tsPlugin from '@typescript-eslint/eslint-plugin';
import tsParser from '@typescript-eslint/parser';
import reactPlugin from 'eslint-plugin-react';
import reactHooksPlugin from 'eslint-plugin-react-hooks';
import reactNativePlugin from 'eslint-plugin-react-native';
import reactNativeA11yPlugin from 'eslint-plugin-react-native-a11y';
import importPlugin from 'eslint-plugin-import';
import unusedImportsPlugin from 'eslint-plugin-unused-imports';
import prettierConfig from 'eslint-config-prettier';
import i18nextPlugin from 'eslint-plugin-i18next';
import jsoncParser from 'jsonc-eslint-parser';
import reactCompilerPlugin from 'eslint-plugin-react-compiler';
// Shared custom ESLint rules — published as @dloizides/frontend-devtools (Platform P1).
// `legacyPlugins` is a Record<ruleName, oneRulePlugin> that preserves the exact
// `'<name>/<name>'` config keys + inline `eslint-disable-next-line <name>` comments.
import { legacyPlugins } from '@dloizides/frontend-devtools/eslint';

export default [
  // Ignore patterns
  {
    ignores: [
      'node_modules/**',
      '.expo/**',
      'dist/**',
      '**/dist/**',
      'build/**',
      'android/**',
      'ios/**',
      '**/*.d.ts',
      'public/**',
      '*.config.js',
      '*.config.mjs',
      'babel.config.js',
      'metro.config.js',
      'lighthouserc.js',
      'scripts/**',
      'src/generated/**',
      // Generated API hooks/endpoints (do not hand-edit)
      'src/server/autoGeneratedHooks/**',
      'src/server/endpoints.ts',
    ],
  },

  // Jest setup (plain JS)
  {
    files: ['jest.setup.js'],
    languageOptions: {
      globals: {
        jest: 'readonly',
        global: 'readonly',
        console: 'readonly',
      },
    },
  },

  // Base JavaScript rules
  js.configs.recommended,

  // TypeScript configuration
  {
    files: ['**/*.ts', '**/*.tsx'],
    languageOptions: {
      parser: tsParser,
      parserOptions: {
        ecmaVersion: 'latest',
        sourceType: 'module',
        ecmaFeatures: {
          jsx: true,
        },
        project: './tsconfig.json',
      },
      globals: {
        // React Native globals
        __DEV__: 'readonly',
        fetch: 'readonly',
        FormData: 'readonly',
        Headers: 'readonly',
        Request: 'readonly',
        Response: 'readonly',
        AbortController: 'readonly',
        Blob: 'readonly',
        File: 'readonly',
        FileReader: 'readonly',
        URL: 'readonly',
        URLSearchParams: 'readonly',
        WebSocket: 'readonly',
        console: 'readonly',
        setTimeout: 'readonly',
        setInterval: 'readonly',
        clearTimeout: 'readonly',
        clearInterval: 'readonly',
        setImmediate: 'readonly',
        clearImmediate: 'readonly',
        requestAnimationFrame: 'readonly',
        cancelAnimationFrame: 'readonly',
        alert: 'readonly',
        process: 'readonly',
        global: 'readonly',
        window: 'readonly',
        document: 'readonly',
        navigator: 'readonly',
        localStorage: 'readonly',
        sessionStorage: 'readonly',
        XMLHttpRequest: 'readonly',
        module: 'readonly',
        require: 'readonly',
        exports: 'readonly',
      },
    },
    plugins: {
      '@typescript-eslint': tsPlugin,
      'react': reactPlugin,
      'react-hooks': reactHooksPlugin,
      'react-native': reactNativePlugin,
      'react-native-a11y': reactNativeA11yPlugin,
      'import': importPlugin,
      'unused-imports': unusedImportsPlugin,
      'react-compiler': reactCompilerPlugin,
      // Shared custom rules (each keyed under its own '<name>' namespace,
      // preserving '<name>/<name>' rule keys + inline disable comments).
      ...legacyPlugins,
    },
    settings: {
      react: {
        version: 'detect',
      },
      'import/resolver': {
        typescript: {
          alwaysTryTypes: true,
        },
      },
    },
    rules: {
      // TypeScript handles undefined identifiers; this rule breaks on TS type-only namespaces like `JSX`
      'no-undef': 'off',

      // =====================================================
      // TYPESCRIPT STRICT RULES - NO ANY ALLOWED!
      // =====================================================
      '@typescript-eslint/no-explicit-any': 'error',
      '@typescript-eslint/no-unsafe-argument': 'error',
      '@typescript-eslint/no-unsafe-assignment': 'error',
      '@typescript-eslint/no-unsafe-call': 'error',
      '@typescript-eslint/no-unsafe-member-access': 'error',
      '@typescript-eslint/no-unsafe-return': 'error',
      '@typescript-eslint/no-unsafe-enum-comparison': 'error',

      // Type safety
      '@typescript-eslint/strict-boolean-expressions': ['error', {
        allowString: false,
        allowNumber: false,
        allowNullableObject: true,
        allowNullableBoolean: false,
        allowNullableString: false,
        allowNullableNumber: false,
        allowAny: false,
      }],
      '@typescript-eslint/no-unnecessary-condition': 'error',
      '@typescript-eslint/no-unnecessary-type-assertion': 'error',
      '@typescript-eslint/no-non-null-assertion': 'error',
      '@typescript-eslint/prefer-nullish-coalescing': 'error',
      '@typescript-eslint/prefer-optional-chain': 'error',
      '@typescript-eslint/switch-exhaustiveness-check': 'error',
      // =====================================================
      // BAN ALL TYPE ASSERTIONS (CASTING)
      // =====================================================
      // Type assertions like `as Record<string, unknown>` bypass TypeScript's
      // type checking. Use proper typing, generics, or type guards instead.
      // Exception: `as const` is still allowed for literal type narrowing.
      // Tests are excluded from this rule (see test file overrides below).
      '@typescript-eslint/consistent-type-assertions': ['error', {
        assertionStyle: 'never',
      }],

      // Function and method rules
      '@typescript-eslint/explicit-function-return-type': ['error', {
        allowExpressions: true,
        allowTypedFunctionExpressions: true,
        allowHigherOrderFunctions: true,
        allowDirectConstAssertionInArrowFunctions: true,
        allowConciseArrowFunctionExpressionsStartingWithVoid: true,
      }],
      '@typescript-eslint/explicit-module-boundary-types': 'error',
      '@typescript-eslint/no-misused-promises': ['error', {
        checksVoidReturn: {
          attributes: false,
        },
      }],
      '@typescript-eslint/no-floating-promises': 'error',
      '@typescript-eslint/promise-function-async': 'error',
      '@typescript-eslint/require-await': 'error',
      '@typescript-eslint/await-thenable': 'error',
      '@typescript-eslint/no-for-in-array': 'error',

      // Variable and declaration rules
      '@typescript-eslint/no-unused-vars': ['error', {
        argsIgnorePattern: '^_',
        varsIgnorePattern: '^_',
        caughtErrorsIgnorePattern: '^_',
      }],
      '@typescript-eslint/no-shadow': 'error',
      '@typescript-eslint/no-use-before-define': ['error', {
        functions: false,
        classes: true,
        variables: true,
      }],
      '@typescript-eslint/no-redeclare': 'error',
      '@typescript-eslint/no-duplicate-enum-values': 'error',

      // Code quality
      '@typescript-eslint/no-unnecessary-type-arguments': 'error',
      '@typescript-eslint/no-redundant-type-constituents': 'error',
      '@typescript-eslint/no-meaningless-void-operator': 'error',
      '@typescript-eslint/no-confusing-void-expression': ['error', {
        ignoreArrowShorthand: true,
      }],
      '@typescript-eslint/no-invalid-void-type': 'error',
      '@typescript-eslint/unified-signatures': 'error',
      '@typescript-eslint/prefer-reduce-type-parameter': 'error',
      '@typescript-eslint/prefer-includes': 'error',
      '@typescript-eslint/prefer-string-starts-ends-with': 'error',
      '@typescript-eslint/prefer-find': 'error',
      '@typescript-eslint/prefer-as-const': 'error',
      '@typescript-eslint/array-type': ['error', { default: 'array-simple' }],

      // Consistency rules
      '@typescript-eslint/consistent-type-imports': ['error', {
        prefer: 'type-imports',
        fixStyle: 'inline-type-imports',
        disallowTypeAnnotations: true,
      }],
      '@typescript-eslint/consistent-type-exports': ['error', {
        fixMixedExportsWithInlineTypeSpecifier: true,
      }],
      '@typescript-eslint/consistent-type-definitions': ['error', 'interface'],
      '@typescript-eslint/consistent-generic-constructors': ['error', 'constructor'],
      '@typescript-eslint/consistent-indexed-object-style': ['error', 'record'],
      '@typescript-eslint/method-signature-style': ['error', 'property'],
      '@typescript-eslint/naming-convention': [
        'error',
        {
          selector: 'default',
          format: ['camelCase'],
          leadingUnderscore: 'allow',
        },
        {
          selector: 'variable',
          format: ['camelCase', 'UPPER_CASE', 'PascalCase'],
          leadingUnderscore: 'allow',
        },
        {
          selector: 'parameter',
          format: ['camelCase'],
          leadingUnderscore: 'allow',
        },
        {
          selector: 'typeLike',
          format: ['PascalCase'],
        },
        {
          selector: 'enumMember',
          format: ['PascalCase', 'UPPER_CASE'],
        },
        {
          selector: 'function',
          format: ['camelCase', 'PascalCase'],
        },
        {
          selector: 'objectLiteralProperty',
          format: null,
        },
        {
          selector: 'typeProperty',
          format: null,
        },
        {
          selector: 'objectLiteralMethod',
          format: ['camelCase', 'PascalCase'],
        },
        {
          selector: 'import',
          format: ['camelCase', 'PascalCase'],
        },
      ],

      // Disable base ESLint rules that are handled by TypeScript
      'no-unused-vars': 'off',
      'no-shadow': 'off',
      'no-use-before-define': 'off',
      'no-redeclare': 'off',
      'require-await': 'off',
      'no-return-await': 'off',

      // =====================================================
      // REACT RULES
      // =====================================================
      'react/jsx-uses-react': 'off', // Not needed with React 17+ JSX transform
      'react/react-in-jsx-scope': 'off', // Not needed with React 17+ JSX transform
      'react/prop-types': 'off', // Using TypeScript for prop validation
      'react/display-name': 'error',
      'react/jsx-key': ['error', { checkFragmentShorthand: true, checkKeyMustBeforeSpread: true }],
      'react/jsx-no-duplicate-props': 'error',
      'react/jsx-no-undef': 'error',
      'react/jsx-pascal-case': 'error',
      'react/jsx-uses-vars': 'error',
      'react/no-children-prop': 'error',
      'react/no-danger-with-children': 'error',
      'react/no-deprecated': 'error',
      'react/no-direct-mutation-state': 'error',
      'react/no-find-dom-node': 'error',
      'react/no-is-mounted': 'error',
      'react/no-render-return-value': 'error',
      'react/no-string-refs': 'error',
      'react/no-unescaped-entities': 'error',
      'react/no-unknown-property': 'error',
      'react/no-unsafe': ['error', { checkAliases: true }],
      'react/self-closing-comp': 'error',
      'react/jsx-boolean-value': ['error', 'never'],
      'react/jsx-curly-brace-presence': ['error', { props: 'never', children: 'never' }],
      'react/jsx-fragments': ['error', 'syntax'],
      'react/jsx-no-useless-fragment': 'error',
      'react/jsx-sort-props': ['error', {
        callbacksLast: true,
        shorthandFirst: true,
        ignoreCase: true,
        reservedFirst: true,
      }],
      'react/function-component-definition': ['error', {
        namedComponents: 'arrow-function',
        unnamedComponents: 'arrow-function',
      }],
      'react/hook-use-state': 'error',
      'react/no-unstable-nested-components': ['error', { allowAsProps: true }],
      'react/no-array-index-key': 'error',
      'react/jsx-no-leaked-render': 'error',
      'react/jsx-no-literals': ['error', {
        noStrings: true,
        allowedStrings: [
          '/', '-', '>', '<', '|', ':', '.', ',', '+', '=', '(', ')',
          '*', '%', '$', '#', '&', '@', '~', '^', '!', '?', ';',
          '\u2192', '\u2715',
        ],
        ignoreProps: true,
        noAttributeStrings: false,
      }],

      // =====================================================
      // REACT HOOKS RULES
      // =====================================================
      'react-hooks/rules-of-hooks': 'error',
      'react-hooks/exhaustive-deps': 'error',

      // =====================================================
      // REACT COMPILER & HOOK STABILITY RULES
      // =====================================================
      // React Compiler ESLint: catches violations of React's Rules
      // (unstable refs in deps, conditional hooks, mutating props, etc.)
      'react-compiler/react-compiler': 'warn',
      // Custom: flags functions/objects/arrays passed to hooks without useCallback/useMemo
      'require-stable-hook-args/require-stable-hook-args': ['warn', {
        ignoreHookPatterns: [
          // Auto-generated React Query hooks (safe - don't use args as deps)
          '^useIdentityService',
          '^useQuestionerWeb',
          '^useOnlineMenu',
          '^useNotification',
        ],
      }],

      // =====================================================
      // REACT NATIVE RULES
      // =====================================================
      'react-native/no-unused-styles': 'error',
      'react-native/split-platform-components': 'error',
      'react-native/no-inline-styles': 'error',
      'react-native/no-color-literals': 'error',
      'react-native/no-raw-text': ['error', {
        skip: ['Text', 'Button'],
      }],
      'react-native/no-single-element-style-arrays': 'error',

      // =====================================================
      // ACCESSIBILITY RULES
      // =====================================================
      'react-native-a11y/has-accessibility-hint': 'error',
      'react-native-a11y/has-accessibility-props': 'error',
      'react-native-a11y/has-valid-accessibility-actions': 'error',
      'react-native-a11y/has-valid-accessibility-role': 'error',
      'react-native-a11y/has-valid-accessibility-state': 'error',
      'react-native-a11y/has-valid-accessibility-states': 'error',
      'react-native-a11y/has-valid-accessibility-component-type': 'error',
      'react-native-a11y/has-valid-accessibility-traits': 'error',
      'react-native-a11y/has-valid-accessibility-value': 'error',
      'react-native-a11y/no-nested-touchables': 'error',
      'react-native-a11y/has-valid-accessibility-descriptors': 'error',
      'react-native-a11y/has-valid-accessibility-ignores-invert-colors': 'error',
      'react-native-a11y/has-valid-accessibility-live-region': 'error',
      'react-native-a11y/has-valid-important-for-accessibility': 'error',

      // =====================================================
      // IMPORT RULES
      // =====================================================
      'import/first': 'error',
      'import/no-duplicates': 'error',
      'import/no-self-import': 'error',
      'import/no-cycle': 'error',
      'import/no-useless-path-segments': 'error',
      'import/newline-after-import': 'error',
      'import/no-mutable-exports': 'error',
      'import/no-default-export': 'off', // React Native needs default exports for screens
      'import/order': ['error', {
        'groups': [
          'builtin',
          'external',
          'internal',
          ['parent', 'sibling'],
          'index',
          'object',
          'type',
        ],
        'pathGroups': [
          {
            pattern: 'react',
            group: 'builtin',
            position: 'before',
          },
          {
            pattern: 'react-native',
            group: 'builtin',
            position: 'before',
          },
          {
            pattern: 'expo**',
            group: 'external',
            position: 'before',
          },
          {
            pattern: '@/**',
            group: 'internal',
            position: 'before',
          },
        ],
        'pathGroupsExcludedImportTypes': ['react', 'react-native'],
        'newlines-between': 'always',
        'alphabetize': {
          order: 'asc',
          caseInsensitive: true,
        },
      }],

      // =====================================================
      // UNUSED IMPORTS
      // =====================================================
      'unused-imports/no-unused-imports': 'error',
      'unused-imports/no-unused-vars': 'off', // Using @typescript-eslint/no-unused-vars
      // NOTE: YAGNI detection (unused exports) is handled by ts-prune CLI tool
      // Run: npm run yagni to check for unused exports
      // The import/no-unused-modules rule doesn't work with ESLint flat config

      // =====================================================
      // FILE AND FUNCTION SIZE LIMITS
      // =====================================================
      // Code standard: Files should be 100-200 lines, max 300
      'max-lines': ['error', {
        max: 200,
        skipBlankLines: true,
        skipComments: true,
      }],
      // Smart function line limits:
      // - React components (returning JSX): max 200 lines
      // - Regular functions: max 50 lines (error), warning above 30 lines
      'smart-max-lines/smart-max-lines': ['error', {
        componentMax: 200,
        functionMax: 50,
        functionWarn: 30,
        skipBlankLines: true,
        skipComments: true,
      }],

      // =====================================================
      // NULL CHECK RULE
      // =====================================================
      // Prohibit direct null checks - use isValueDefined() instead
      // This ensures consistent handling of both null and undefined
      'no-null-check/no-null-check': 'error',

      // =====================================================
      // ENUM ENFORCEMENT (custom plugins)
      // =====================================================
      'prefer-const-enum/prefer-const-enum': 'warn',
      'enum-file-isolation/enum-file-isolation': 'warn',

      // =====================================================
      // BARREL FILE CONFLICT PREVENTION
      // =====================================================
      // Flags foo.ts when foo/index.ts also exists (TS resolves to file, ignoring barrel)
      'no-barrel-companion-file/no-barrel-companion-file': 'error',

      // =====================================================
      // OPTIONAL + UNDEFINED TYPE PREVENTION
      // =====================================================
      // Prohibit `?: T | undefined` — use `?: T` and omit the key instead
      'no-optional-undefined/no-optional-undefined': 'error',

      // =====================================================
      // GENERATED MODEL BARREL PROTECTION
      // =====================================================
      // Prevents pulling entire generated model barrels into the runtime graph.
      // Type-only imports are allowed; runtime imports must target leaf modules.
      'no-generated-models-barrel-value-import/no-generated-models-barrel-value-import': 'error',

      // =====================================================
      // MODULE STRUCTURE & TEST COLOCATION
      // =====================================================
      // Enforce hooks → hooks/, sub-components → components/, utils → utils/
      // Only when directory has 4+ source files
      'enforce-module-structure/enforce-module-structure': ['error', { fileThreshold: 4 }],
      // Test files must sit next to source, not in __tests__/ dirs
      'enforce-test-colocation/enforce-test-colocation': 'error',

      // =====================================================
      // FUNCTION STYLE & ORDERING
      // =====================================================
      // Multi-statement functions → function declaration (not const arrow)
      // Module ordering: exports → private functions → constants
      'enforce-function-style/enforce-function-style': ['warn', {
        checkStyle: true,
        checkOrdering: true,
      }],

      // =====================================================
      // COMPONENT REUSABILITY
      // =====================================================
      // Ban product-specific imports in src/components/ (shared code)
      // Set to warn until existing violations are migrated to feature modules
      'no-product-imports-in-shared/no-product-imports-in-shared': 'warn',
      // Warn when product modules create components that duplicate shared ones
      'no-duplicate-shared-patterns/no-duplicate-shared-patterns': 'warn',

      // =====================================================
      // TRANSLATION ENFORCEMENT - FM() only, no t()
      // =====================================================
      // Ban useTranslation hook from react-i18next.
      // Use FM() from localization/helpers instead — it has no fallback
      // mechanism, so missing keys are immediately visible in the UI.
      'no-restricted-imports': ['error', {
        paths: [{
          name: 'react-i18next',
          importNames: ['useTranslation'],
          message: 'Use FM() from localization/helpers instead of t() from useTranslation(). FM() has no fallback strings — missing keys are immediately visible.',
        }],
      }],

      // =====================================================
      // GENERAL BEST PRACTICES
      // =====================================================
      'no-console': ['error', { allow: ['warn', 'error'] }],
      'no-debugger': 'error',
      'no-alert': 'error',
      'no-eval': 'error',
      'no-implied-eval': 'error',
      'no-new-func': 'error',
      'no-script-url': 'error',
      'no-void': 'error',
      'no-with': 'error',
      'no-proto': 'error',
      'no-extend-native': 'error',
      'no-iterator': 'error',
      'no-labels': 'error',
      'no-lone-blocks': 'error',
      'no-multi-str': 'error',
      'no-new-wrappers': 'error',
      'no-octal-escape': 'error',
      'no-self-compare': 'error',
      'no-sequences': 'error',
      'no-throw-literal': 'error',
      'no-unmodified-loop-condition': 'error',
      'no-unused-expressions': 'error',
      'no-useless-call': 'error',
      'no-useless-concat': 'error',
      'no-useless-return': 'error',
      'prefer-promise-reject-errors': 'error',
      'radix': 'error',
      'yoda': 'error',
      'eqeqeq': ['error', 'always'],
      'curly': ['error', 'multi'],
      'default-case': 'error',
      'default-case-last': 'error',
      'grouped-accessor-pairs': 'error',
      'no-constructor-return': 'error',
      'no-else-return': 'error',
      'no-empty-function': ['error', { allow: ['arrowFunctions'] }],
      'no-extra-bind': 'error',
      'no-floating-decimal': 'error',
      'no-implicit-coercion': 'error',
      'no-multi-spaces': 'error',
      'no-param-reassign': ['error', { props: true }],
      'no-return-assign': 'error',
      'prefer-const': 'error',
      'prefer-template': 'error',
      'object-shorthand': 'error',
      'no-var': 'error',
      'no-nested-ternary': 'error',
      'no-unneeded-ternary': 'error',
      'spaced-comment': ['error', 'always'],
      'max-depth': ['error', 4],
      'max-nested-callbacks': ['error', 3],
      'max-params': ['error', 4],
      'complexity': ['warn', 15],
      // Code standard: Use enums/constants instead of magic values
      'no-magic-numbers': ['error', {
        ignore: [0, 1, -1, 2, 100],
        ignoreArrayIndexes: true,
        ignoreDefaultValues: true,
        enforceConst: true,
        ignoreClassFieldInitialValues: true,
        ignoreEnums: true,
      }],
      'no-restricted-syntax': [
        'error',
        {
          selector: 'TSEnumDeclaration:not([const])',
          message: 'Use const enum instead of enum. String literal union types should also use const enum.',
        },
        {
          selector: 'IfStatement > LogicalExpression[left.type="LogicalExpression"]',
          message: 'Condition has more than 2 expressions. Extract to a named variable (e.g., const shouldUpdate = a && b && c)',
        },
        {
          selector: 'IfStatement > LogicalExpression[right.type="LogicalExpression"]',
          message: 'Condition has more than 2 expressions. Extract to a named variable (e.g., const shouldUpdate = a && b && c)',
        },
        {
          selector: 'ConditionalExpression > LogicalExpression[left.type="LogicalExpression"]',
          message: 'Ternary condition has more than 2 expressions. Extract to a named variable',
        },
        {
          selector: 'ConditionalExpression > LogicalExpression[right.type="LogicalExpression"]',
          message: 'Ternary condition has more than 2 expressions. Extract to a named variable',
        },
        {
          selector: 'WhileStatement > LogicalExpression[left.type="LogicalExpression"]',
          message: 'While condition has more than 2 expressions. Extract to a named variable',
        },
        {
          selector: 'WhileStatement > LogicalExpression[right.type="LogicalExpression"]',
          message: 'While condition has more than 2 expressions. Extract to a named variable',
        },
      ],
    },
  },

  // =====================================================
  // I18N ENFORCEMENT - No hardcoded user-facing strings
  // =====================================================
  {
    files: ['**/*.ts', '**/*.tsx'],
    plugins: { 'i18next': i18nextPlugin },
    rules: {
      'i18next/no-literal-string': ['error', {
        markupOnly: true,
        ignoreAttribute: [
          'className', 'style', 'id', 'key', 'ref', 'type', 'name', 'value',
          'defaultValue', 'href', 'src', 'as', 'to', 'from',
          'mode', 'variant', 'size', 'color',
          'testID',
          'accessibilityRole', 'accessibilityState',
          'aria-hidden', 'aria-live', 'aria-current', 'aria-expanded',
          'aria-selected', 'aria-checked',
          'd', 'viewBox', 'fill', 'stroke', 'strokeWidth',
          'strokeLinecap', 'strokeLinejoin', 'xmlns',
          'role', 'tabIndex', 'htmlFor', 'autoComplete', 'autoFocus',
          'autoCapitalize', 'keyboardType', 'returnKeyType',
          'secureTextEntry', 'multiline', 'numberOfLines',
          'resizeMode', 'contentFit', 'pointerEvents',
        ],
        ignoreCallee: [
          'require', 'Error', 'URL', 'console.*', 'cn',
          'navigate', 'useRoute', 'useNavigation',
          'StyleSheet.create', 'Dimensions.get',
        ],
        ignoreProperty: [
          'className', 'style', 'testID', 'id', 'key', 'type', 'name',
          'variant', 'mode', 'path', 'uri', 'source',
          'fontFamily', 'fontWeight', 'textAlign', 'display',
          'flex', 'flexDirection', 'alignItems', 'justifyContent',
        ],
        ignore: [
          '^[A-Z_]+$',
          '^#[0-9a-fA-F]+$',
          '^rgba?\\(',
          '^M\\d',
        ],
      }],
    },
  },

  // Configuration overrides for test files
  {
    files: ['**/*.test.ts', '**/*.test.tsx', '**/*.spec.ts', '**/*.spec.tsx', '**/__tests__/**'],
    languageOptions: {
      globals: {
        describe: 'readonly',
        it: 'readonly',
        test: 'readonly',
        expect: 'readonly',
        jest: 'readonly',
        beforeAll: 'readonly',
        beforeEach: 'readonly',
        afterAll: 'readonly',
        afterEach: 'readonly',
      },
    },
    rules: {
      '@typescript-eslint/no-explicit-any': 'warn', // Slightly relaxed for tests
      '@typescript-eslint/no-non-null-assertion': 'off',
      '@typescript-eslint/no-unsafe-argument': 'off',
      '@typescript-eslint/no-unsafe-assignment': 'off',
      '@typescript-eslint/no-unsafe-call': 'off',
      '@typescript-eslint/no-unsafe-member-access': 'off',
      '@typescript-eslint/no-unsafe-return': 'off',
      '@typescript-eslint/require-await': 'off',
      // Allow type assertions in tests - mocking often requires casting
      '@typescript-eslint/consistent-type-assertions': ['error', {
        assertionStyle: 'as',
        objectLiteralTypeAssertions: 'allow-as-parameter',
      }],
      'max-lines': 'off', // Test files can be longer due to setup/assertions/fixtures
      'max-nested-callbacks': 'off',
      'smart-max-lines/smart-max-lines': 'off', // Tests can be longer due to setup/assertions
      'no-magic-numbers': 'off', // Tests often use literal values for assertions
      'no-script-url': 'off',
      'no-console': 'off',
      'no-null-check/no-null-check': 'off',
      'prefer-const-enum/prefer-const-enum': 'off',
      'enum-file-isolation/enum-file-isolation': 'off',
      'react/jsx-no-literals': 'off', // Tests often have hardcoded strings in assertions
      'i18next/no-literal-string': 'off', // Tests use hardcoded strings
      'react-compiler/react-compiler': 'off',
      'require-stable-hook-args/require-stable-hook-args': 'off',
      'enforce-module-structure/enforce-module-structure': 'off',
      'enforce-function-style/enforce-function-style': 'off',
      'no-restricted-imports': 'off', // Tests may mock useTranslation
      'no-product-imports-in-shared/no-product-imports-in-shared': 'off',
      'no-duplicate-shared-patterns/no-duplicate-shared-patterns': 'off',
    },
  },

  // Allow console usage in the centralized logger utility only
  {
    files: ['src/utils/logger.ts'],
    rules: {
      'no-console': 'off',
    },
  },

  // =====================================================
  // I18N INTERPOLATION - Enforce {{p1}}/{{p2}}/{{p3}} in translation JSON
  // =====================================================
  {
    files: ['src/localization/locales/**/*.json'],
    languageOptions: {
      parser: jsoncParser,
    },
    plugins: {
      ...legacyPlugins,
    },
    rules: {
      'i18n-interpolation/i18n-interpolation': 'error',
    },
  },

  // =====================================================
  // I18N PARAM NAMES - Enforce p1/p2/p3 in t() call sites
  // =====================================================
  {
    files: ['src/**/*.ts', 'src/**/*.tsx'],
    ignores: ['src/**/__tests__/**', 'src/**/*.test.*'],
    plugins: {
      ...legacyPlugins,
    },
    rules: {
      'i18n-param-names/i18n-param-names': 'error',
    },
  },

  // =====================================================
  // ENFORCE ROUTE PAGE PRELOADING
  // =====================================================
  {
    files: ['src/config/routePreloader.ts'],
    plugins: {
      ...legacyPlugins,
    },
    rules: {
      'enforce-route-preload/enforce-route-preload': ['error', {
        routeDir: 'app/(protected)',
        excludeFiles: ['_layout.tsx'],
        excludeDirs: ['__tests__'],
        rootIndexIsRedirect: true,
      }],
    },
  },

  // =====================================================
  // SIDEBAR NAV ROUTE/KEY UNIQUENESS
  // =====================================================
  {
    files: ['packages/*/src/index.ts'],
    plugins: {
      ...legacyPlugins,
    },
    rules: {
      'no-duplicate-nav-prefix/no-duplicate-nav-prefix': 'error',
    },
  },

  // Prettier config (overrides formatting rules)
  prettierConfig,

  // Re-enable curly rule after Prettier (Prettier disables it, but we want to enforce single-line style)
  {
    files: ['**/*.ts', '**/*.tsx'],
    rules: {
      // Enforce no braces for single-statement if/else/while/for blocks
      // This makes code more concise: if (x) return y; instead of if (x) { return y; }
      'curly': ['error', 'multi'],
    },
  },
];
