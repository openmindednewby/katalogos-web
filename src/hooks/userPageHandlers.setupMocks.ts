/**
 * Shared mock setup for useUserPageHandlers tests.
 */

// Mock dependencies
jest.mock('../localization/helpers', () => ({
  FM: jest.fn((id: string, p1?: string) => (typeof p1 === 'string' ? `${id}:${p1}` : id)),
}));

jest.mock('../lib/notifications', () => ({
  notify: jest.fn(),
  notifySuccess: jest.fn(),
}));

jest.mock('../utils/logger', () => ({
  logger: {
    error: jest.fn(),
    warn: jest.fn(),
    info: jest.fn(),
  },
}));

jest.mock('react-native', () => ({
  Alert: {
    alert: jest.fn(),
  },
}));

// Mock the auto-generated hooks
jest.mock(
  '../server/autoGeneratedHooks/identity/tenants/tenants',
  () => ({
    useIdentityServiceAPITenantsListTenants: jest.fn(() => ({
      data: { tenants: [] },
      isLoading: false,
      refetch: jest.fn().mockResolvedValue({}),
    })),
  })
);

jest.mock('../server/autoGeneratedHooks/identity/users/users', () => ({
  useIdentityServiceAPIUsersListUsers: jest.fn(() => ({
    data: { users: [] },
    isLoading: false,
    refetch: jest.fn().mockResolvedValue({}),
  })),
  useIdentityServiceAPIUsersCreateUser: jest.fn(() => ({
    mutate: jest.fn(),
    isLoading: false,
  })),
  useIdentityServiceAPIUsersDeleteUser: jest.fn(() => ({
    mutate: jest.fn(),
    isLoading: false,
  })),
  useIdentityServiceAPIUsersSetUserEnabled: jest.fn(() => ({
    mutate: jest.fn(),
    isLoading: false,
  })),
  useIdentityServiceAPIUsersUpdatePassword: jest.fn(() => ({
    mutate: jest.fn(),
    isLoading: false,
  })),
}));

jest.mock('./useGetRole', () => ({
  useGetRole: jest.fn(() => ({
    isSuperAdmin: true,
    isTenantAdmin: false,
    role: 'superAdmin',
  })),
}));

export {};
