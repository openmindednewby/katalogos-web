/** Placement options for tooltip positioning relative to the target element. */
const enum TooltipPlacement {
  Top = 'top',
  Bottom = 'bottom',
  Left = 'left',
  Right = 'right',
}

export default TooltipPlacement;
