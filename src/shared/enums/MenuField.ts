/** Menu fields that can be mapped to import file columns. */
const enum MenuField {
  Category = 'category',
  ItemName = 'itemName',
  Description = 'description',
  Price = 'price',
  Unmapped = 'unmapped',
}

export default MenuField;
