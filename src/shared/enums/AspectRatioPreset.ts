/** Aspect ratio presets for image cropping. */
const enum AspectRatioPreset {
  Square = 'square',
  Landscape = 'landscape',
  Classic = 'classic',
  Free = 'free',
}

export default AspectRatioPreset;
