/** Tabs available in the full menu editor. */
const enum EditorTab {
  Metadata = 'metadata',
  Content = 'content',
  Preview = 'preview',
  Translations = 'translations',
  History = 'history',
}

export default EditorTab;
