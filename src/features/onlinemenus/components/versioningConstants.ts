/**
 * Shared layout constants for versioning components.
 * Used by VersionHistoryPanel, VersionDetailView, VersionDiffView, and RestoreConfirmModal.
 */

/** Standard container padding. */
export const CONTAINER_PADDING = 16;

/** Header row bottom margin. */
export const HEADER_MARGIN_BOTTOM = 16;

/** Title font size. */
export const TITLE_FONT_SIZE = 18;

/** Summary text font size. */
export const SUMMARY_FONT_SIZE = 14;

/** Summary text bottom margin. */
export const SUMMARY_MARGIN_BOTTOM = 16;

/** Title bottom margin in list views. */
export const TITLE_MARGIN_BOTTOM = 12;

/** Version number font size. */
export const VERSION_NUMBER_FONT_SIZE = 16;

/** Version date font size. */
export const VERSION_DATE_FONT_SIZE = 13;

/** Version date top margin. */
export const VERSION_DATE_MARGIN_TOP = 4;

/** Badge horizontal padding. */
export const BADGE_PADDING_H = 8;

/** Badge vertical padding. */
export const BADGE_PADDING_V = 2;

/** Badge border radius. */
export const BADGE_RADIUS = 4;

/** Badge font size. */
export const BADGE_FONT_SIZE = 11;

/** Badge font weight. */
export const BADGE_FONT_WEIGHT = '700' as const;

/** Standard button border radius. */
export const BUTTON_RADIUS = 8;

/** Version row vertical padding. */
export const VERSION_ROW_PADDING = 12;

/** Load-more button top margin. */
export const LOAD_MORE_MARGIN_TOP = 16;

/** Load-more button vertical padding. */
export const LOAD_MORE_PADDING_V = 10;

/** Load-more button horizontal padding. */
export const LOAD_MORE_PADDING_H = 20;

/** Empty text top margin. */
export const EMPTY_TEXT_MARGIN_TOP = 24;

/** Empty text font size. */
export const EMPTY_TEXT_FONT_SIZE = 14;

/** Entry container padding. */
export const ENTRY_PADDING = 12;

/** Entry container border radius. */
export const ENTRY_RADIUS = 8;

/** Entry container bottom margin. */
export const ENTRY_MARGIN_BOTTOM = 8;

/** Entry path font size. */
export const ENTRY_FONT_SIZE = 14;

/** Entry path font weight. */
export const ENTRY_FONT_WEIGHT = '600' as const;

/** Entry path bottom margin. */
export const ENTRY_PATH_MARGIN_BOTTOM = 4;

/** Entry badge bottom margin. */
export const ENTRY_BADGE_MARGIN_BOTTOM = 6;

/** Max value display length. */
export const MAX_VALUE_LENGTH = 80;

/** Value label width. */
export const VALUE_LABEL_WIDTH = 40;

/** Value label font weight. */
export const VALUE_LABEL_FONT_WEIGHT = '600' as const;

/** Value text font size. */
export const VALUE_FONT_SIZE = 13;

/** Value row top margin. */
export const VALUE_MARGIN_TOP = 4;

/** Back button vertical padding. */
export const BACK_BUTTON_PADDING_V = 8;

/** Back button horizontal padding. */
export const BACK_BUTTON_PADDING_H = 12;

/** Back button right margin. */
export const BACK_BUTTON_MARGIN_RIGHT = 12;

/** Action row gap. */
export const ACTION_ROW_GAP = 12;

/** Action row bottom margin. */
export const ACTION_ROW_MARGIN_BOTTOM = 16;

/** Action button vertical padding. */
export const ACTION_BUTTON_PADDING_V = 10;

/** Action button horizontal padding. */
export const ACTION_BUTTON_PADDING_H = 16;

/** Action text font size. */
export const ACTION_TEXT_FONT_SIZE = 14;

/** Snapshot container padding. */
export const SNAPSHOT_PADDING = 12;

/** Snapshot container border radius. */
export const SNAPSHOT_RADIUS = 8;

/** Snapshot font size. */
export const SNAPSHOT_FONT_SIZE = 13;

/** Max snapshot display lines. */
export const MAX_SNAPSHOT_LINES = 20;

/** Snapshot title font size. */
export const SNAPSHOT_TITLE_SIZE = 16;

/** Snapshot title bottom margin. */
export const SNAPSHOT_TITLE_MARGIN = 8;

/** Modal overlay color. */
export { MODAL_OVERLAY_COLOR } from '@/shared/constants';

/** Modal dialog width. */
export const MODAL_WIDTH = 400;

/** Modal border radius. */
export const MODAL_RADIUS = 12;

/** Modal inner padding. */
export const MODAL_PADDING = 24;

/** Modal message bottom margin. */
export const MODAL_MESSAGE_MARGIN_BOTTOM = 20;

/** Modal buttons gap. */
export const MODAL_BUTTONS_GAP = 12;
