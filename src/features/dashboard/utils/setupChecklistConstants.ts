/** localStorage key for tracking QR code generation. */
export const CHECKLIST_QR_GENERATED_KEY = 'checklist_qr_generated';

/** localStorage key for tracking menu sharing. */
export const CHECKLIST_MENU_SHARED_KEY = 'checklist_menu_shared';

/** localStorage key for checklist dismissal. */
export const CHECKLIST_DISMISSED_KEY = 'checklist_dismissed';

/** Total number of steps in the setup checklist. */
export const TOTAL_CHECKLIST_STEPS = 5;

/** Percentage multiplier for progress bar width calculation. */
export const PERCENTAGE_MULTIPLIER = 100;
