/** Position of price relative to item content. */
const enum PricePosition {
  Right = 'right',
  BelowName = 'below-name',
  BelowDescription = 'below-description',
  Badge = 'badge',
}

export default PricePosition;
