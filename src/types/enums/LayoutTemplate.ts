/** Available layout templates for menu display. */
const enum LayoutTemplate {
  ModernGrid = 'modern-grid',
  ClassicList = 'classic-list',
  Cards = 'cards',
  Compact = 'compact',
  Elegant = 'elegant',
}

export default LayoutTemplate;
