/** Media display size options. */
const enum MediaSize {
  Thumbnail = 'thumbnail',
  Small = 'small',
  Medium = 'medium',
  Large = 'large',
  Full = 'full',
  Custom = 'custom',
}

export default MediaSize;
