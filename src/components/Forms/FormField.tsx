/**
 * Re-export of the shared FormField from @dloizides/ui-forms (Capability Wave C1, batch 2).
 * Theme is supplied app-wide via FeedbackUiAdapter (@dloizides/ui-feedback context).
 */
export { FormField } from '@dloizides/ui-forms';
