/** Border radius for plan cards. */
export const PLAN_CARD_BORDER_RADIUS = 12;

/** Border width for the highlighted (current/popular) plan card. */
export const HIGHLIGHTED_BORDER_WIDTH = 2;

/** Default border width for plan cards. */
export const DEFAULT_BORDER_WIDTH = 1;

/** Padding inside plan cards. */
export const PLAN_CARD_PADDING = 20;

/** Font size for plan name headings. */
export const PLAN_NAME_FONT_SIZE = 18;

/** Font size for price display. */
export const PLAN_PRICE_FONT_SIZE = 32;

/** Font size for small price labels (like /mo). */
export const PRICE_LABEL_FONT_SIZE = 14;

/** Font size for feature list items. */
export const FEATURE_FONT_SIZE = 14;

/** Font size for billing history table cells. */
export const TABLE_CELL_FONT_SIZE = 13;

/** Padding for table cells. */
export const TABLE_CELL_PADDING = 10;

/** Watermark font size. */
export const WATERMARK_FONT_SIZE = 11;

/** Watermark vertical padding. */
export const WATERMARK_PADDING = 8;

/** Minimum width for plan comparison cards. */
export const PLAN_CARD_MIN_WIDTH = 260;

/** Margin below the price row. */
export const PRICE_ROW_MARGIN_BOTTOM = 4;

/** Left margin for the billing period label. */
export const PERIOD_MARGIN_LEFT = 2;

/** Height of horizontal divider lines. */
export const DIVIDER_HEIGHT = 1;

/** Vertical margin for divider lines. */
export const DIVIDER_MARGIN_V = 16;

/** Gap between feature row items. */
export const FEATURE_ROW_GAP = 8;

/** Top margin for checkmark alignment. */
export const CHECKMARK_MARGIN_TOP = 1;

/** Line height for feature text. */
export const FEATURE_LINE_HEIGHT = 20;

/** Margin below section headers. */
export const HEADER_MARGIN_BOTTOM = 12;

/** Checkmark symbol for feature lists. */
export const CHECKMARK_SYMBOL = '\u2713';
