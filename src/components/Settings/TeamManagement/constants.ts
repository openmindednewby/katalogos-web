/**
 * Constants for TeamManagement screens.
 */

/** Height of each row in team member and invitation lists. */
export const ROW_PADDING_VERTICAL = 12;

/** Font size for member/invitation email text. */
export const EMAIL_FONT_SIZE = 14;

/** Font size for secondary info text (role badge, dates). */
export const SECONDARY_FONT_SIZE = 12;

/** Spacing between action buttons in a row. */
export const ACTION_BUTTON_GAP = 8;

/** Border radius for role badges. */
export const BADGE_BORDER_RADIUS = 4;

/** Padding inside role badges. */
export const BADGE_PADDING_HORIZONTAL = 8;

/** Vertical padding inside role badges. */
export const BADGE_PADDING_VERTICAL = 2;

/** Font weight for role badges. */
export const BADGE_FONT_WEIGHT = '600' as const;

/** Modal max width. */
export const MODAL_MAX_WIDTH = 420;

/** Modal padding. */
export const MODAL_PADDING = 20;

/** Modal border radius. */
export const MODAL_BORDER_RADIUS = 12;

/** Small spacing between meta elements. */
export const META_MARGIN_TOP = 4;

/** Border width for inputs and cards. */
export const BORDER_WIDTH = 1;

/** Modal title font size. */
export const MODAL_TITLE_FONT_SIZE = 18;

/** Modal title font weight. */
export const MODAL_TITLE_FONT_WEIGHT = '600' as const;

/** Modal title bottom margin. */
export const MODAL_TITLE_MARGIN_BOTTOM = 16;

/** Label bottom margin in modals. */
export const LABEL_MARGIN_BOTTOM = 4;

/** Input padding inside modals. */
export const INPUT_PADDING = 10;

/** Input bottom margin in modals. */
export const INPUT_MARGIN_BOTTOM = 16;

/** Modal button vertical padding. */
export const BUTTON_PADDING_VERTICAL = 10;

/** Modal button horizontal padding. */
export const BUTTON_PADDING_HORIZONTAL = 20;

/** Modal button minimum width. */
export const BUTTON_MIN_WIDTH = 80;

/** Disabled element opacity. */
export { DISABLED_OPACITY } from '../../../shared/constants';
