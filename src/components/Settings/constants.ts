/**
 * Shared style constants for all Settings sub-screens.
 * Domain-specific constants live in each sub-directory's own constants.ts.
 */

/** Spacing between top-level sections. */
export const SECTION_SPACING = 16;

/** Small vertical gap between elements. */
export const SMALL_SPACING = 8;

/** Medium vertical gap between elements. */
export const MEDIUM_SPACING = 12;

/** Minimal gap between title and subtitle. */
export const TITLE_GAP = 4;

/** Font size for section titles. */
export const TITLE_FONT_SIZE = 16;

/** Font size for standard body/status text. */
export const BODY_FONT_SIZE = 14;

/** Font size for description and secondary text. */
export const DESCRIPTION_FONT_SIZE = 13;

/** Margin top for error text below loading indicators. */
export const ERROR_TEXT_MARGIN_TOP = 8;

/** Border radius for input fields. */
export const INPUT_BORDER_RADIUS = 6;

/** Padding inside input fields. */
export const INPUT_PADDING = 10;

/** Standard border width for input fields. */
export const INPUT_BORDER_WIDTH = 1;
