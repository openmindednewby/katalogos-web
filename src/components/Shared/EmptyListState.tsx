/**
 * Re-export of the shared EmptyListState from @dloizides/ui-feedback (Capability Wave C1).
 * Theme + i18n + navigation are supplied app-wide via FeedbackUiAdapter / FeedbackUiProvider.
 */
export { EmptyListState as default } from '@dloizides/ui-feedback';
