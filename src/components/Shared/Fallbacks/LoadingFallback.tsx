/**
 * Re-export of the shared LoadingFallback from @dloizides/ui-feedback (Capability Wave C1).
 * Theme + i18n are supplied app-wide via FeedbackUiAdapter / FeedbackUiProvider.
 */
export { LoadingFallback as default } from '@dloizides/ui-feedback';
