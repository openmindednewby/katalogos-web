/**
 * Re-export of the shared PageSkeleton from @dloizides/ui-feedback (Capability Wave C1).
 * Theme + i18n are supplied app-wide via FeedbackUiAdapter / FeedbackUiProvider.
 */
export { PageSkeleton as default } from '@dloizides/ui-feedback';
