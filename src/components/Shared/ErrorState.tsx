/**
 * Re-export of the shared ErrorState from @dloizides/ui-feedback (Capability Wave C1).
 * Theme + i18n are supplied app-wide via FeedbackUiAdapter / FeedbackUiProvider.
 */
export { ErrorState as default } from '@dloizides/ui-feedback';
