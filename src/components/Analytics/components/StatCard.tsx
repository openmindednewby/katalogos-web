/**
 * Re-export of the shared StatCard from @dloizides/ui-tables (Capability Wave C1, batch 3).
 * Theme + i18n are supplied app-wide via FeedbackUiAdapter (@dloizides/ui-feedback context).
 */
export { StatCard as default } from '@dloizides/ui-tables';
